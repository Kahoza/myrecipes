<nav class="navbar sticky-top navbar-expand-lg " style="background-color: #FFFFFF;">
  <%= link_to "MyRecipes", root_path, class: "navbar-brand", id: "logo"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="dropdown">
      <ul>
      <ul class="nav-item dropdown navbar"></ul>
       <a class="nav-link dropdown-toggle a-navbar" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Ingredients</a>
       <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        <ul>
          <%= link_to "Ingredients", ingredients_path, class: 'dropdown-item btn-css-class', style: 'color:black' %>
        </ul>
          <% Ingredient.all.each do |ingredient| %>
            <ul class="navbar-text"><%= link_to ingredient.name.capitalize, ingredient_path(ingredient), class:'dropdown-item btn-css-class', style: 'color:black' %></ul>
          <% end %>
          <% if logged_in? && current_chef.admin? %>
            <div class="dropdown-divider"></div>
              <ul><%= link_to  "New ingredient",  new_ingredient_path, class: 'dropdown-item btn-css-class', style: 'color:black'  %></ul>
          <% end %>
      </div>
      </ul>
    </div>
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item active">
        <a class="nav-link a-navbar" href="/chefs">Chefs <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link a-navbar" href="/recipes">Recipes <span class="sr-only">(current)</span></a>
      </li>
    </ul>


    <div class="dropdown">
    <ul>
      <ul class="nav-item dropdown navbar-right"></ul>
      <% if logged_in? %>
      <a class="nav-link dropdown-toggle a-navbar" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_chef.chefname.upcase %>'s profile <small><%= "Admin" if current_chef.admin? %></small></a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <ul>
          <%= link_to "My profile", chef_path(current_chef), class: 'navbar-text btn-css-class', style: 'color:black' %>
        </ul>
        <ul>
          <%= link_to "Edit my profile", edit_chef_path(current_chef), class: 'navbar-text btn-css-class', style: 'color:black' %>
        </ul>
        <ul>
          <%= link_to "Add a new recipe", new_recipe_path, class: 'navbar-text btn-css-class', style: 'color:black' %>
        </ul>
        <div class="dropdown-divider"></div>
          <ul><%= link_to  "Log out", logout_path, method: :delete, class: 'navbar-text btn-css-class', style: 'color:black'  %></ul>
          <% else %>
          <a class="nav-link a-navbar navbar-text" href="/login">Login <span class="sr-only"></span></a>
        <% end %>
      </div>
    </ul>
  </div>
  </div>
</nav>
